{% extends 'base.html.twig' %}

{% block title %}Business{% endblock %}

{% block body %}
    <h1>Business</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ business.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ business.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ business.address }}</td>
            </tr>
            <tr>
                <th>Phone_contact</th>
                <td>{{ business.phoneContact }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ business.description }}</td>
            </tr>
            <tr>
                <th>Statistic</th>
                <td>{{ business.statistic }}</td>
            </tr>
            <tr>
                <th>Carbon_footprint</th>
                <td>{{ business.carbonFootprint }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_business_c_r_u_d_index') }}">back to list</a>

    <a href="{{ path('app_business_c_r_u_d_edit', {'id': business.id}) }}">edit</a>

    {{ include('business_crud/_delete_form.html.twig') }}
{% endblock %}
